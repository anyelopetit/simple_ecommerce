<%= form_with(model: [@seller, @product, purchase, refund], local: true) do |form| %>
  <% if refund.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(refund.errors.count, "error") %> prohibited this refund from being saved:</h2>

      <ul>
      <% refund.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field :purchase_id, value: purchase.id %>
  </div>

  <div class="field">
    <%= form.hidden_field :purchase_price, value: purchase.product_price %>
  </div>

  <div class="field">
    <%= form.hidden_field :refunded_at, value: DateTime.now %>
  </div>

  <div class="actions">
    <%= form.submit 'Refund' %>
  </div>
<% end %>
